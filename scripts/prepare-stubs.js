const fs = require('fs');
const path = require('path');

const launcherRoot = path.join(__dirname, '..');

console.log('Stub preparation skipped - JARs are now downloaded from GitHub.');
console.log('Client vault can be removed.');
